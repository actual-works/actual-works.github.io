# AGENTS.md — ACTUAL WORKS 実行仕様 v3.0

> Codex・Claude Code 等のエージェントが参照する実行仕様。
> ブランド定義の正本は `owner.md` を優先する。本ファイルは「どう動くか」を定義する。
> 最終更新：2026-06-29

---

## 1. エージェント共通の基本姿勢

```
- 煽らない／事実と推測を分ける／検証できることだけ言う
- ツールより業務設計を優先／実行手順と合格基準を渡す
- AIの限界を隠さない／最終判断と責任は人間が持つ
- 動かす → 測る → 直す → 広げる（広げるは最後）
```

---

## 2. ファイル操作ルール

1. 対象ファイルを**先に読んでから**編集する
2. 依頼された範囲**だけ**変更する。無関係なファイルを触らない
3. 差分を確認し、未解決事項は正直に報告する
4. 新規ファイルより既存ファイルの編集を優先する
5. コメントは「なぜ」が自明でない場合のみ書く。Whatは書かない

---

## 3. 禁止事項（エージェント共通）

- 旧ブランド名（Protocol Zero / P-ZERO 等）を使う
- 実名・勤務先・個人特定属性を出力に含める
- 本業の社内事例・実データ・社内画面を参照・生成する
- 成果保証・収益保証・完全自動化を示唆する表現を生成する
- 出所不明の数字・捏造データを生成する
- AI出力を未検証で公開用成果物として扱う
- 成立していない業務を自動化する
- 正本（`owner.md`）より古い出力・記憶を優先する

---

## 4. スキル一覧と呼び出し規約

| スキル名 | 用途 | 主な入力 |
|---|---|---|
| `actualworks-note-writer` | note記事執筆（A/B型） | テーマ・型指定（A or B）・対象読者層（A/B/C） |
| `actualworks-x-writer` | X単一投稿＋画像プロンプト生成 | テーマ・投稿の狙い（問題提起/反応テスト/導線） |
| `daihon` | ショート動画台本（3〜7分） | テーマ・一動画一テーマ |
| `img` | 画像生成プロンプト | 用途・サイズ・メッセージ |
| `skill-md-generator` | SKILL.md 自動生成メタスキル | スキル名・用途・入出力定義 |

### 呼び出し時の共通確認事項
- 記事タイプ（A/B）は明示されているか
- 対象読者層（A未着手・B未設計・C未仕組み化）は明示されているか
- 既出記事マップ（`owner.md` §23相当）と重複しないか
- 媒体（note / X）が確定しているか

---

## 5. コンテンツ生成ルール

### チャネルの役割（絶対に混同しない）

| 媒体 | 渡すもの | 読者の変化 |
|---|---|---|
| **X 長文記事** | 考え方・マインド・哲学 | 「そういうことか」共感・納得・認識変容 |
| **note 記事** | 具体的な実行手順・型 | 「これをやれば動ける」行動・再現 |

---

### X 長文記事

**目的**：読者の認識を変える。「なぜやるか・何が本質か・どう考えるか」を渡す。

**構成（順番は柔軟に）**：
1. 違和感・問題提起（読者がうなずける出発点）
2. 一般的な誤解・よくある思い込み
3. 本質の問い（核心）
4. 視点の転換・考え方の再定義
5. 具体的な思考例・失敗談・裏話（括弧で本音を差し込む）
6. 読者が今日から変えられる「認識」

**文体ルール**：
- 敬語ベースの崩した話し口調（「〜なんですよね」「〜ですけど」「それで、」）
- **太字（`**`）・イコール（=）・矢印（→）禁止**
- 論理は「つまり」「だから」「でも」で繋ぐ
- 括弧（）で本音・裏話・自虐を差し込む
- 一次情報（実体験・失敗談）必須
- 誘導リンク・「詳しくは」・「noteで解説」は書かない
- 促音は全体で2〜3箇所まで
- 数字・製品仕様は公式情報で裏取り

**1投稿あたりの分量**：長文（複数スクロール可）。ただし冗長にしない。

---

### note 記事

**目的**：読者が実際に一つの業務を動かせる状態にする。「何をどの順番でやるか・どう判断するか」を渡す。

**構成**：
1. 何が手に入るか・誰向けか（冒頭で明示）
2. 効く条件・前提・対象読者
3. 実行手順（番号付き・具体的な操作レベル）
4. テンプレート（コピペ可・`【】`差し替え形式）
5. 合格基準（「これができたらOK」の判断基準）
6. 失敗パターンと回避策
7. チェックリスト＋次の一手
8. 署名クローザー

**必須要素（最低1つ）**：実行手順／テンプレート／合格基準／チェックリスト／失敗時の修正方法

**文体ルール**：
- 一人称は「自分」。文末は「〜んです」「〜と思っています」基調
- 誠実・語りかけ。煽り・誇張なし
- 末尾に署名クローザー必須：**AIは道具だ。設計なしに、道具は機能しない。**

---

### 画像・図解（共通）
- カラー：`#1568F0`（ブランドブルー）を基本アクセントに統一
- アスペクト比：16:9
- 一枚一メッセージ／グラデ・影・3Dなし／右上に「AW」モノグラム

---

## 6. 品質ゲート（公開・納品前チェック）

以下をすべて確認してから成果物を渡す。

```
[ ] 目的・対象読者・成果物範囲が明確か
[ ] 必須要素（手順／判断基準／記入例／テンプレ のうち最低1つ）があるか
[ ] 事実確認完了か（変化しやすい情報は最新性を確認）
[ ] 禁止語・禁止表現がないか
[ ] 旧ブランド名が含まれていないか
[ ] 個人・勤務先が特定・推定されないか
[ ] 本業フローが再現されないか
[ ] 読者の次の行動が明確か
[ ] 媒体固有ルール（署名・文体）が守られているか
[ ] 既出記事との重複がないか（あれば内部リンクで委譲）
```

---

## 7. 自動化の原則

```
手動で成立 → 手順記録 → 変数と固定ルール分離 → 品質基準設定
→ 半自動化 → 人間レビュー → 測定 → 低リスク範囲だけ自動化
```

**成立していない業務を自動化しない。**

Memory（Codex等）：補助層として使用可。正本はMarkdownへ固定。
匿名性・本業機密・禁止表現・ブランド名・法務・品質ゲートはMemory依存させない。

---

## 8. 制作インフラ

- **設計・推論**：Claude Code
- **並列実行**：Codex（マルチエージェント構成）
- **環境**：Chromebook Plus ＋ Linux（Crostini）、Claude Pro

---

## 9. 短縮版システムプロンプト（各エージェント初期化用）

```
あなたは、日本の実務向けAI実装ブランド「ACTUAL WORKS」の制作・運用を支援します。

核となる思想：「AIは道具だ。設計なしに、道具は機能しない。」

目的：公開情報と個人環境で再現可能な検証を、AI初心者が仕事・副業・発信で使える
手順・判断基準・テンプレート・チェックリスト・SOPへ変換する。

主な読者：日本の30〜50代のビジネスパーソン。AIに関心はあるが、何から始めるか、
どう業務へ組み込むか、どう品質を判断するか分からない人。

絶対に守る：
- ブランド名はACTUAL WORKS（旧名 Protocol Zero 等は使用しない）
- 実名・顔・勤務先・個人情報を出さない
- 本業の社内業務・実データ・社内画面・知財を出さない
- 成果保証・収益保証・完全自動化・放置運営を示唆しない
- AI出力を未検証で公開しない

品質優先順位：1.機密/法務/知財 → 2.事実性 → 3.ブランド整合 →
4.読者が実行できること → 5.再現性 → 6.分かりやすさ → 7.見た目
```

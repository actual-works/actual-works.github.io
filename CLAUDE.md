# CLAUDE.md — ACTUAL WORKS 制作・運用ガイド

> このファイルは Claude Code が参照するプロジェクト設定。ブランド定義の正本は `owner.md` / `ACTUAL_WORKS_MASTER.md` を優先する。

---

## 1. ブランド概要

**ACTUAL WORKS**（半角英大文字表記が原則）

AI知識を「実際に使える型」に変換する、30〜50代実務家向けの**AI実務実装ブランド**。

芯：**「AIは道具だ。設計なしに、道具は機能しない。」**

- 旧名（使用禁止）：Protocol Zero / P-ZERO / プロトコル・ゼロ / Pzero
- X：@actual_works
- note：note.com/actual_works
- 運営者：TAKAFUMI（匿名。個人特定属性の組み合わせは出さない）

---

## 2. 指示の優先順位

1. 法令・規約・機密保持・個人情報・知的財産の保護
2. オーナーが現在のタスクで明示した最新指示
3. 正本（`owner.md` / `ACTUAL_WORKS_MASTER.md`）
4. 本ファイル → `AGENTS.md` → `prompts/` → `templates/` → 過去成果物 → AIの推測

過去の出力やAIの記憶を、承認済みの正本ルールより優先しない。

---

## 3. 絶対に守る禁則

- 旧ブランド名（Protocol Zero / P-ZERO 等）を使わない
- 実名・顔・勤務先・職位・居住地・個人アカウントに結びつく情報を出さない
- 本業の社内事例・業務プロセス・実データ・社内画面・知財を出さない
- 成果保証・収益保証・完全自動化・放置運営を示唆しない
- 煽り・誇張・「稼げる」断定（誰でも／秒で／絶対稼げる）を使わない
- 過剰表現（100%／絶対／革命／最強）を使わない
- 出所不明・裏が取れない数字を載せない／捏造しない
- AI出力を未検証で公開しない
- noteの型（太字・署名）をXに持ち込まない
- 同一内容を全媒体へそのままコピペしない
- 人間側フレームワークを乱立させない（5ソフトスキルに統一）

---

## 4. ファイル役割分担

```
owner.md / ACTUAL_WORKS_MASTER.md  → ブランド・事業・思想・制約の正本（最上位）
CLAUDE.md                           → Claude Code 設定（本ファイル）
AGENTS.md                           → Codex のプロジェクト内挙動
prompts/*.md                        → 媒体・作業別の生成手順
templates/*.md                      → 成果物の出力形式
quality_check.md                    → 公開・納品前の検査基準
operations/*.md                     → 運用手順・判断履歴・測定・メモリ方針
```

---

## 5. カスタムスキル

| スキル名 | 用途 |
|---|---|
| `actualworks-note-writer` | note執筆（A/B型） |
| `actualworks-x-writer` | X単一投稿＋画像プロンプト生成 |
| `daihon` | ショート動画台本 |
| `img` | 画像生成プロンプト |
| `skill-md-generator` | SKILL.md 自動生成メタスキル |

---

## 6. コンテンツの型

記事はA/Bどちらかに振り分ける。**1記事＝1つの中心メッセージ。**

### A：実装ガイド型（読者に「今日やること」を渡す）
何が手に入るか → 効く条件・前提 → 実装手順 → テンプレート集 → 計測・検証 → 失敗パターンと回避 → チェックリスト＋次の一手

### B：原則・問題提起エッセイ型（読者の「考え方」を変える）
共感フック → 核心の問い（太字） → 問題の構造 → 答え → 内部リンク → 反論への目配り → まとめ

---

## 7. 媒体別文体ルール

### note
- 一人称は「自分」。文末は「〜んです」「〜と思っています」基調
- 誠実・語りかけ。煽り・誇張なし
- 末尾に署名クローザー必須：**AIは道具だ。設計なしに、道具は機能しない。**

### X（@actual_works）
- 敬語ベースの崩した話し口調（「〜なんですよね」「〜ですけど」）
- **太字・`**`・イコール（=）・矢印（→）は禁止**
- 括弧（）で本音・裏話・自虐を差し込む
- 誘導リンク・「詳しくは」は一切なし
- 促音は1投稿2〜3箇所まで

---

## 8. デザイン仕様（図解・画像）

### カラーパレット（全媒体統一）
| 役割 | カラーコード |
|---|---|
| ブランドブルー（基準・アクセント） | `#1568F0` |
| ネイビー | `#0B1F3A` |
| アクセントブルー（補助） | `#3A8DF0` |
| 薄いブルー（背景・面） | `#E8F1FB` |
| 白 | `#FFFFFF` |

### X投稿図解の固定仕様
- アスペクト比：**16:9**
- 純白背景／細いグレーライン／右上に小さく「AW」モノグラム
- グラデ・影・3Dなし／余白たっぷり
- アクセントは `#1568F0` の一色
- 図中ラベルは実際の日本語をゴシック体で正確に描画

### 主な画像サイズ
- note・OGP系：`1280×670`
- 16:9記事画像：`1280×720`

---

## 9. 品質基準

### 品質の優先順位
1. 機密保持 → 2. 法令・規約・知財 → 3. 事実性 → 4. ブランド整合 → 5. 読者が実行できること → 6. 再現性 → 7. 分かりやすさ → 8. 見た目 → 9. 文章の磨き込み

### Definition of Done
- 目的・対象読者・成果物範囲が明確
- 必須要素がそろう（手順／判断基準／記入例／テンプレ／失敗時の修正／今日やる作業 のうち最低1つ）
- 事実確認完了・禁止事項チェック完了
- 読者の次の行動が明確
- 公開物は匿名性監査完了

---

## 10. ファイル編集の原則

- 対象ファイルを先に読んでから編集する
- 依頼範囲だけ変更し、無関係なファイルを触らない
- 差分を確認し、未解決事項は正直に報告する
- 成立していない業務を自動化しない

---

## 11. 最終判断基準

> **これは、現実の読者がAIを使って一つの現実的な業務を動かし、結果を判断し、修正し、安全に再現する助けになっているか。**

答えが「いいえ」なら、ACTUAL WORKS としては未完成。
